module.exports = {
  env: {
    browser: true,
    es2021: true,
  },
  extends: [
    'plugin:react/recommended',
    'airbnb',
  ],
  parserOptions: {
    ecmaFeatures: {
      jsx: true,
    },
    ecmaVersion: 12,
    sourceType: 'module',
  },
  plugins: [
    'react',
  ],
  rules: {
    'react/jsx-filename-extension': [1, { extensions: ['.js', '.jsx'] }],
  },
};

// script "eslint:init" on package.json to init eslint >>> npm run eslint:init

// script "lint" on package.json to check the ECMAScript / JavaScript pattern >>> npm run lint

// crtl + shift + p >>> eslint fix auto
